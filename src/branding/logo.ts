import fullLogoUrl from '@/assets/brand/full-logo.png';
import logoMarkUrl from '@/assets/brand/logo-mark.png';

/** URLs empaquetadas por Vite (fiables en dev y build). */
export const APP_LOGO_FULL_SRC = fullLogoUrl;
export const APP_LOGO_MARK_SRC = logoMarkUrl;

/** Fallback público por si se necesita fuera de React. */
export const APP_LOGO_PUBLIC_SRC = '/assets/logos/FullLogo_Transparent.png';

export const APP_LOGO_ALT = 'Parking SaaS';
